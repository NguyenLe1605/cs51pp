#pragma once

#include <string>
using std::string;

auto hello_lab1() -> void;

auto square(int x) -> int;

auto exclaim(string text) -> string;

auto needs_small_bills(int price) -> bool;

auto computus_month(int year) -> int;

auto computus_day(int year) -> int;

auto frustrum_volume(float radius1, float radius2, float height) -> float;

auto factorial(int x) -> int;

auto sum_from_zero(int x) -> int;
